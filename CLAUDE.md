# Agent Instructions

## Git Branch Naming

Always prefix new branch names with the current date in `YYYY-MM-DD` format so that branches can be discovered easily by date.

Example: `2026-02-08/feature-name`
